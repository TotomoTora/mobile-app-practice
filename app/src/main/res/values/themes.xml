<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Обычная тема приложения (без ActionBar) -->
    <style name="Theme.practice.App" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- Тема для системного SplashScreen -->
    <style name="Theme.practice" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/ic_launcher_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.practice.App</item>
    </style>

</resources>
